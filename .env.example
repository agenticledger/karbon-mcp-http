# Server port (default: 3100)
PORT=3100

# No API keys needed — clients pass their own Karbon credentials via headers.
# Karbon requires two credentials:
#   - Access Key (JWT) — sent as X-Karbon-Access-Key header
#   - Bearer Token (tenant key) — sent as Authorization: Bearer <token>
